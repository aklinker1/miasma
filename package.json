{
  "name": "miasma",
  "version": "0.4.0",
  "scripts": {
    "build": "./scripts/server-build.sh",
    "preview": "./scripts/server-preview.sh",
    "dev": "concurrently --kill-others-on-fail -n JS,GO -c green,cyan \"pnpm -q dev:frontend\" \"pnpm -q dev:backend\"",
    "dev:backend": "nodemon -q --signal SIGTERM -e go --exec 'bash -c' ./scripts/server-dev-backend.sh",
    "dev:frontend": "cd web && pnpm dev",
    "gen": "go generate ./...",
    "clean": "sudo rm -rf data/apps.*",
    "cli:build": "./scripts/cli-build.sh",
    "docs:gen": "./scripts/docs-gen.sh",
    "docs:build": "pnpm docs:gen && cd docs && pnpm build",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky install"
  },
  "devDependencies": {
    "concurrently": "^7.1.0",
    "husky": "^7.0.4",
    "nodemon": "^2.0.16",
    "prettier": "^2.6.2",
    "pretty-quick": "^3.1.3"
  }
}
